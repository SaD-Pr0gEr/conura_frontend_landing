# Conura Frontend (Next.js + TypeScript)

Современный фронтенд для сайта Conura Digital Agency, построенный на React, Next.js 16 и TypeScript.

## 🛠 Технологии

- **Next.js 16** - React фреймворк с SSR/SSG
- **TypeScript** - типизация
- **Tailwind CSS 4** - стилизация
- **App Router** - маршрутизация Next.js 13+

## 📁 Структура проекта

```
frontend/
├── src/
│   ├── app/                    # Страницы (App Router)
│   │   ├── page.tsx           # Главная страница
│   │   ├── bots/              # Страница "Боты"
│   │   └── context/           # Страница "Контекстная реклама"
│   ├── components/
│   │   ├── ui/                # UI компоненты (Button, Input, Modal)
│   │   ├── layout/            # Layout компоненты (Header, Footer)
│   │   ├── sections/          # Секции страниц (Banner, Services, Cases)
│   │   └── forms/             # Формы (ContactFormModal)
│   ├── hooks/                 # React hooks
│   ├── lib/                   # Утилиты
│   ├── types/                 # TypeScript типы
│   └── data/                  # Данные и константы
├── public/                    # Статические файлы
├── next.config.ts            # Конфигурация Next.js
├── tailwind.config.ts        # Конфигурация Tailwind
└── tsconfig.json             # Конфигурация TypeScript
```

## 🚀 Запуск

### Development

```bash
cd frontend
npm install
npm run dev
```

Открыть [http://localhost:3000](http://localhost:3000)

### Production Build

```bash
npm run build
npm run start
```

## 📦 Доступные команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск в режиме разработки |
| `npm run build` | Сборка для production |
| `npm run start` | Запуск production сервера |
| `npm run lint` | Проверка ESLint |

## 🎨 Компоненты

### UI Компоненты (`/components/ui`)
- `Button` - кнопки (primary, secondary, white, outline, round)
- `Container` - контейнер с отступами
- `Modal` - модальное окно
- `Input` - поле ввода
- `Select` - выпадающий список

### Layout (`/components/layout`)
- `Header` - шапка сайта с навигацией
- `Footer` - подвал сайта
- `MainLayout` - общий layout со всеми элементами

### Секции (`/components/sections`)
- `BannerSlider` - слайдер баннеров
- `Services` - блок услуг с табами
- `HowWeWork` - как мы работаем
- `Cases` - кейсы
- `CTA` - призыв к действию
- `Blog` - блог
- `ITPartner` - резидент IT Park

## 🪝 Hooks

- `useModal` - управление модальными окнами
- `useSlider` - логика слайдера
- `useMediaQuery` - медиа-запросы
- `useIsMobile` / `useIsDesktop` - определение устройства

## 🎯 SEO

Проект оптимизирован для SEO:
- Metadata API в каждой странице
- Open Graph теги
- Семантическая разметка
- Server-Side Rendering (SSR)

## 📝 Добавление новых страниц

1. Создать папку в `src/app/[page-name]/`
2. Создать `page.tsx` с metadata и контентом
3. Добавить страницу в навигацию (`data/navigation.ts`)

## 🔗 Связь с Production

Этот проект является **тестовой версией** для проверки SEO и новых технологий.
Текущий production сайт находится в папке `/src` корня проекта.
